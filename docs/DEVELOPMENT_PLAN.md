# SeaControll 开发计划

## 总目标

构建一个面向 `ESP32 / ESP01S` 的可配置 IoT 控制平台，支持：

- 多账户
- 用户名密码登录
- 登录后绑定 Passkey
- 控制器链接码绑定
- 驱动化设备能力
- GPIO 绑定
- 读写分离的通道模型
- 定时器
- 通用公式与规则执行引擎
- 后续共享驱动市场

## 当前阶段

### 已确认的产品约束

- 平台管理端只做管理员侧能力
- 平台管理端只看用户列表、用户详情、用户设备列表
- 用户端 `pages` 只做普通用户侧能力
- `admin` 与 `pages` 都必须先鉴权，再访问业务数据
- 未鉴权状态只允许显示登录/注册/引导，不允许直接展示列表与配置
- 设备驱动配置、GPIO 配置、定时器配置属于用户侧，不属于平台管理端首页职责

### 阶段 1：本地开发环境与基础骨架

- [x] Go 后端基础 API
- [x] Pages 控制前端
- [x] 设备端 AP 配网骨架
- [x] 用户名密码登录与控制器链接码绑定骨架
- [x] 后端 Docker 本地开发环境
- [x] PostgreSQL 初始表结构

### 阶段 2：驱动化配置模型

- [x] 定义驱动库模型
- [x] 设备实例挂载驱动接口骨架
- [x] 驱动实例映射运行时能力
- [x] GPIO 绑定冲突校验
- [x] 上电默认状态模型
- [x] 读写 schema 统一化

### 阶段 3：内置驱动首批可用

- [x] 内置继电器驱动
- [x] 内置造浪泵驱动
- [x] 内置温度计驱动
- [x] 状态读取标准化
- [x] 命令写入标准化
- [x] 固件内置功能目录

### 阶段 4：数据库正式接管业务数据

- [x] 账户改为 PostgreSQL 持久化
- [x] 设备改为 PostgreSQL 持久化
- [x] 控制器链接码改为 PostgreSQL 持久化
- [x] 设备所有权改为 PostgreSQL 持久化
- [x] 定时器改为 PostgreSQL 持久化
- [x] 驱动实例改为 PostgreSQL 持久化
- [x] 设备状态快照改为 PostgreSQL 持久化

### 阶段 5：共享市场

- [x] 市场包元数据
- [x] 版本管理
- [x] 发布/下架
- [x] 安装记录
- [x] 兼容性与审核机制

### 阶段 6：访问控制与后台分域

- [x] 管理员角色模型
- [x] 普通用户角色模型
- [x] `admin` 鉴权拦截
- [x] `pages` 鉴权拦截
- [x] `admin` 多页面骨架
- [x] 管理员用户列表接口
- [x] 管理员用户设备列表接口
- [x] 普通用户自有设备接口边界收敛
- [x] 用户设备列表页
- [x] 用户设备详情页
- [x] 用户驱动实例配置页
- [ ] 用户设置页
- [ ] 用户修改密码
- [ ] 登录后绑定 Passkey 页面入口

### 阶段 7：通用公式与规则执行引擎

- [x] 定义设备侧公式引擎边界
- [x] 定义通用 `JSON DSL`
- [x] 定义变量 / 输入源模型
- [x] 定义基础数学算子
- [x] 定义条件判断与状态切换
- [x] 定义输出绑定模型
- [x] 定义 `tick` 执行周期
- [x] 定义设备状态回报格式
- [x] 仿真端实现公式执行器 `v1`
- [x] 后端持久化公式配置
- [x] 用户端配置页面 `v1`

## 下一阶段拆解

### 7.1 公式引擎 `v1`

目标：先做最小可用版本，支撑造浪与温度换算。

- [x] 支持常量输入
- [x] 支持模块状态输入
- [x] 支持传感器输入
- [x] 支持用户参数输入
- [x] 支持 `+ - * /`
- [x] 支持 `min max clamp`
- [x] 支持 `if`
- [x] 支持 `> < >= <= == !=`
- [x] 支持变量读写
- [x] 支持上限 / 下限反向

### 7.2 造浪模块接入公式引擎

- [ ] 把造浪模块从固定模式切到公式模式
- [ ] 保留快捷模式作为公式模板
- [ ] 支持单通道渐强渐弱
- [ ] 支持单通道周期波动
- [ ] 支持双通道交替输出
- [ ] 支持定时切换公式

### 7.3 温度模块接入公式引擎

- [x] 支持 `C -> F`
- [x] 支持线性校准
- [x] 支持偏移校准
- [x] 支持结果字段映射
- [x] 支持告警阈值判断

## 近期开发优先级

1. 定义公式引擎 `JSON DSL v1`
2. 在仿真端实现公式执行器 `v1`
3. 让造浪模块切换为公式驱动
4. 让温度模块接入简单公式换算
5. 补用户设置页（修改密码 / 绑定 Passkey）

## 数据存储策略

- 主业务：`PostgreSQL`
- 灵活配置：`JSONB`
- 共享市场：同库分表
- 当前阶段不引入 Redis / 对象存储 / 搜索引擎

## 本地测试方式

- `cd backend && docker compose up --build`
- 后端：`http://localhost:8080`
- 管理端：`http://localhost:8080/admin`
- 数据库：`localhost:5432`
- Adminer：`http://localhost:8081`
- 前端：本地静态服务单独启动
